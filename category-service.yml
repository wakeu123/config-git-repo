management:
  endpoints:
    web:
      exposure:
        include: '*'

spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:category-db

server:
  servlet:
    context-path: /api